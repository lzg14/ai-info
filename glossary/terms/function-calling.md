### Function Calling

**英文：** Function Calling

**解释：**

Function Calling 是大模型的一种能力——让模型在生成文本时，能够主动调用外部函数/工具（如查询天气、搜索网页、操作数据库），而不是只能输出文字。模型输出一个结构化的"函数调用请求"，由外部程序执行后返回结果，模型再继续生成。

**为什么重要：** Function Calling 是 Agent 架构的基础能力之一，使得 AI 能从"聊天"进化到"做事"。
